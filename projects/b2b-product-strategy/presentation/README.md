# Presentation assets

| File | Purpose |
|---|---|
| `AI_Customer_Service_Competitive_Teardown_v3.pptx` | Four-vendor competitive teardown + strategic synthesis (wedge, moat, land/expand) |
| `B2B_Product_Strategy_Story_Deck.pptx` | Story + appendix deck covering the full strategy (all 5 assignment steps), styled to match `nykaa-ux-analysis/Nykaa-UX-Analysis.pptx` |
| `scripts/deck_lib.py`, `scripts/build_story_deck.py` | Generates `B2B_Product_Strategy_Story_Deck.pptx` via python-pptx. Re-run with `python3 scripts/build_story_deck.py` from `presentation/` after editing content in the script. |

## Status

Portfolio artifact, not a submission requirement. The assignment brief calls for PDF + video only (see `../assignment-brief.md`); this deck does not block the 12 September submission. Every fact in it is pulled from `../final/Airtribe_B2B_Product_Strategy_Final.pdf` and the teardown deck above — nothing new was introduced, and the same evidence-boundary language (hypothesis vs. supported vs. unproven) is preserved throughout.
